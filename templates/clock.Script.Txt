//Script for generating a clock in the pyplanet interface
declare CMLabel clock_text;
main(){
  while(True){
    declase Text ct = currentTime;
    (Page.GetFirstChild("myLabel5") as CMLabel).SetText(ct);
    yield;
  }
}
